1. sticker_to_rgb565.py file that takes an image (sticker) and changes it to rgb565 format

2. csv_to_image_rescue.py takes csv as input (dsl -> csv) and reconstructs image

3. sticker_to_dvo_csv encodes an image (a sticker) to csv identical to exported csv from a dsl file.
